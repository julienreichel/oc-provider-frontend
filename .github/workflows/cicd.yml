name: Frontend CI/CD

on:
  push:
    branches: ['main', 'dev']
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: julienreichel/oc-infra/.github/workflows/reusable-build.yml@main
    with:
      image_name: ghcr.io/julienreichel/oc-provider-frontend
    secrets: inherit

  deploy-prod:
    if: github.ref_name == 'main'
    uses: julienreichel/oc-infra/.github/workflows/reusable-deploy.yml@main
    with:
      app_kind: frontend
      image_name: ghcr.io/julienreichel/oc-provider-frontend
      namespace: oc-provider
      host: provider.on-track.ch
    secrets: inherit

  deploy-dev:
    if: github.ref_name == 'dev'
    uses: julienreichel/oc-infra/.github/workflows/reusable-deploy.yml@main
    with:
      app_kind: frontend
      image_name: ghcr.io/julienreichel/oc-provider-frontend
      namespace: oc-dev-provider
      host: dev.provider.on-track.ch
    secrets: inherit
