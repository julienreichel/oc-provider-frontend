apiVersion: apps/v1
kind: Deployment
metadata:
  name: oc-provider-frontend
  namespace: oc-provider
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oc-provider-frontend
  template:
    metadata:
      labels:
        app: oc-provider-frontend
    spec:
      containers:
        - name: app
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: oc-provider-frontend
  namespace: oc-provider
spec:
  selector:
    app: oc-provider-frontend
  ports:
    - port: 80
      targetPort: 80
